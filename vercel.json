{
  "buildCommand": "pnpm --filter @amore/frontend build",
  "outputDirectory": "apps/frontend/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
